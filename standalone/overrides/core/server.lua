--- Overrides bridge functions
--- Do not overwrite the bridge files.
--- Copy the function you want to modify here

-- function eCore:createCallback(name, callback)
-- function eCore:createUsableItem(item, cb)

-- messages
-- function eCore:hideText(source) end
-- function eCore:drawText(source, message, position, mType) end
-- function eCore:sendMessage(source, message, mType, mSec) end

-- accounts
-- function eCore:addMoney(xPlayer, account, amount, reason) end
-- function eCore:removeMoney(xPlayer, accountName, amount, reason) end
-- function eCore:getAccounts(xPlayer, account) end

-- function eCore:getPlayer(playerId)
-- function eCore:convertPlayer(playerData) end

